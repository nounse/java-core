package com.nounse.core.security;

public class Role {
    // We must use ROLE_ prefix here, otherwise things won't work
    public static final String USER = "ROLE_USER";
    public static final String ADMIN = "ROLE_ADMIN";
}
